1) Run REIDS model to obtain the parameter estimates. This can be done item by item (see SingleItems) or in parallel (see Parallel).

2) Filter probe sets on exon scores and perform t-test on the array scores with .R

3) Rank the identified probe sets based on the junction information with .R
